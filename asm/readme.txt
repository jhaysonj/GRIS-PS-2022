assembler --> nasm

para compilar --> nasm -f elf64 assembly.nasm

para linkar --> ld assembly.o

para executar --> ./a.out

antes de executar digite o comando --> chmod +x tag

todos os comandos ao mesmo tempo --> nasm -f elf64 assembly.nasm && ld assembly.o && ./a.out



